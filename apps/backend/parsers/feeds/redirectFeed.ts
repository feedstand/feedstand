import { FeedParser } from '../../types/system'

export const redirectFeed: FeedParser = async () => {
    // TODO: Implement checking the headers of the response and redirecting to the provided
    // location and then parsing the feed.

    return undefined
}
