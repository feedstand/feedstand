name: feedstand

services:
  frontend:
    build:
      context: ..
      dockerfile: apps/frontend/.docker/Dockerfile.test
    volumes:
      - ..:/app
      - /app/node_modules

  backend:
    build:
      context: ..
      dockerfile: apps/backend/.docker/Dockerfile.test
    volumes:
      - ..:/app
      - /app/node_modules
